# Trendslop AI-assisted fix instructions

> ⚠️ This was an AI-assisted fix attempt that fell back to instruction-only mode.
> Fallback reason: `Claude rated confidence LOW — won't ship code`

## Missing authentication on API routes

**Severity:** HIGH
**Check ID:** `missing_auth_on_routes`
**Summary:** 4 state-changing route(s) with no auth check.

---

## Claude's analysis and recommended fix

## What's missing

No authentication mechanism is visible anywhere in the provided files. The codebase shows:
- Two Express routers (`routes/admin.js` and `queries.js`)
- No auth middleware imports (`passport`, `jsonwebtoken`, `express-session`, etc.)
- No auth helper functions
- No `req.user` checks
- No bearer token validation
- No session checks

Without seeing how this app handles authentication elsewhere (login routes, middleware files, environment variables with JWT secrets, database user tables), I cannot apply the same pattern.

## What you need to do

1. **Find your auth mechanism.** Look for:
   - `middleware/auth.js` or similar
   - Login routes that set `req.session` or issue JWTs
   - Imports like `const jwt = require('jsonwebtoken')` or `const session = require('express-session')`
   - Environment variables like `JWT_SECRET`

2. **Apply it to these routes.** For Express, the pattern is usually:
   ```javascript
   const requireAuth = require('./middleware/auth'); // your actual auth middleware
   
   router.delete('/admin/users/:id', requireAuth, (req, res) => {
     // route logic
   });
   ```

3. **Add admin checks** since these are `/admin/*` routes. If your auth middleware sets `req.user`, verify:
   ```javascript
   router.delete('/admin/users/:id', requireAuth, (req, res) => {
     if (!req.user.isAdmin) {
       return res.status(403).json({ error: 'Admin access required' });
     }
     // delete logic
   });
   ```

4. **Don't protect unauthenticated endpoints** like `/login`, `/signup`, health checks, or webhook receivers (which use HMAC signatures instead).

If you're starting from scratch with no auth, consider `express-session` + bcrypt for password hashing, or a hosted solution like Clerk/Auth0. But that's a larger architectural decision beyond fixing these specific routes.

---

## Next steps

1. Read Claude's analysis above
2. Apply the changes yourself, in this branch or your main branch
3. Re-run a Trendslop audit to confirm the finding has passed
4. Close this PR when done

---

*Generated by [Trendslop](https://trendslop.ai). Need help? Contact support.*
