# Trendslop AI-assisted fix instructions

> ⚠️ This was an AI-assisted fix attempt that fell back to instruction-only mode.
> Fallback reason: `Claude rated confidence LOW — won't ship code`

## Secrets in client-side code

**Severity:** HIGH
**Check ID:** `secrets_in_client`
**Summary:** 1 secret(s) embedded in client-side code.

---

## Claude's analysis and recommended fix

## Critical Security Issue: Anthropic API Key Exposed in Browser

**File:** `public/checkout.html:13`  
**Secret Type:** Anthropic API key (`sk-ant-...`)  
**Risk Level:** CRITICAL

### What an attacker can do with this key:

- **Rack up unlimited API charges** on your Anthropic account (Claude API calls can cost hundreds of dollars per day if abused)
- **Exhaust your rate limits**, making your legitimate app unusable
- **Extract proprietary prompts** if your production code uses system prompts that reveal business logic
- **Use your quota** for their own applications

Because this key is in a static HTML file served from `/public`, every single user who has ever loaded this page has downloaded it. Even if you remove it now, it's already in browser caches, archives, and browser history across the internet.

---

## Step 1: ROTATE THE SECRET IMMEDIATELY

⚠️ **Do this FIRST, before any code changes:**

1. Log into the [Anthropic Console](https://console.anthropic.com/)
2. Navigate to API Keys settings
3. **Revoke** the key `sk-ant-FAKEKEY-trendslop-test-fixtures-not-real-abcdef0123456789`
4. Generate a new key
5. **Do NOT put the new key anywhere in `/public` or client-side code**

The old key is compromised. Rotating ensures attackers can't keep using it after you fix the code.

---

## Step 2: Understand the Architecture Change

You cannot call the Anthropic API directly from the browser. The fix requires:

1. **Backend proxy endpoint** that holds the secret server-side
2. **Client code** calls YOUR server instead of Anthropic's API
3. **Secret storage** in environment variables (never in code)

---

## Step 3: Create a Backend Proxy Endpoint

Since you're serving static files from `/public`, you likely have one of these stacks:

### Option A: Node.js / Express Backend

Create a new file `server.js` (or add to your existing Express app):



**Install dependencies:**
bash
ANTHROPIC_API_KEY=sk-ant-YOUR-NEW-KEY-HERE node server.js
```

### Option B: Next.js API Route

If you're using Next.js, create `pages/api/chat.js`:



**⚠️ Next.js gotcha:** Variables prefixed with `NEXT_PUBLIC_` are PUBLIC (embedded in the browser bundle). For secrets, use unprefixed variables — they only exist server-side.

### Option C: Serverless Function (Vercel, Netlify, Cloudflare Workers)

Create `api/chat.js` (Vercel/Netlify) or a Worker script:



---

## Step 4: Update the Client Code

Replace `public/checkout.html` with:



---

## Step 5: Store the Secret Securely

### Local Development

Create a `.env` file in your project root (and add `.env` to `.gitignore`):

```
ANTHROPIC_API_KEY=sk-ant-YOUR-NEW-KEY-HERE
```

### Production Deployment

**Vercel:**
bash
heroku config:set ANTHROPIC_API_KEY=sk-ant-YOUR-NEW-KEY-HERE
```

**AWS / Docker:**
Use AWS Secrets Manager or pass as environment variable in your container config.

---

## Step 6: Verify the Fix

1. **Check your browser DevTools** (Network tab) — you should see a request to `/api/chat`, NOT to `api.anthropic.com`
2. **Inspect the JavaScript source** — search for `sk-ant` or `ANTHROPIC`. It should NOT appear anywhere.
3. **Test in an incognito window** to ensure no cached code is running

---

## Additional Hardening (Optional but Recommended)

### Rate Limiting

Add rate limiting to your proxy to prevent abuse:



### Input Validation

Validate the user's messages before forwarding to Anthropic:



---

## Why This Can't Be Auto-Fixed

This vulnerability requires:
- Creating new backend infrastructure (the proxy endpoint)
- Configuring environment variables on your hosting provider
- Understanding your existing backend stack (Express vs Next.js vs serverless)
- Testing the entire request flow

There's no way to generate a production-ready code diff without knowing your deployment setup. That's why this is marked `CONFIDENCE: LOW` — you need to implement these instructions manually based on your specific architecture.

---

## Next steps

1. Read Claude's analysis above
2. Apply the changes yourself, in this branch or your main branch
3. Re-run a Trendslop audit to confirm the finding has passed
4. Close this PR when done

---

*Generated by [Trendslop](https://trendslop.ai). Need help? Contact support.*
