# Trendslop AI-assisted fix instructions

> ⚠️ This was an AI-assisted fix attempt that fell back to instruction-only mode.
> Fallback reason: `Claude rated confidence LOW — won't ship code`

## Missing authentication on API routes

**Severity:** HIGH
**Check ID:** `missing_auth_on_routes`
**Summary:** 4 state-changing route(s) with no auth check.

---

## Claude's analysis and recommended fix

## Explanation

I cannot find any authentication mechanism in the provided code files. Neither `routes/admin.js` nor `queries.js` contain any auth middleware, helper functions, or imports of auth libraries. Without seeing:

- How other routes in this Express app handle authentication
- Whether there's a middleware file (e.g., `middleware/auth.js`)
- Which auth library they use (Passport, JWT, express-session, custom)
- How user sessions/tokens are validated

I cannot write production-grade code that matches their existing auth pattern.

## What you need to do

1. **Find your auth mechanism**: Look for files like:
   - `middleware/auth.js` or `middleware/requireAuth.js`
   - Other route files that DO have auth (check `routes/users.js`, `routes/auth.js`)
   - `app.js` or `server.js` for session middleware setup
   - Package.json to see if you use `passport`, `jsonwebtoken`, `express-session`, etc.

2. **For Express with per-route middleware** (most common pattern):
   ```javascript
   const requireAuth = require('../middleware/auth'); // your auth function
   
   router.delete('/admin/users/:id', requireAuth, requireAdmin, (req, res) => {
     // Now req.user is guaranteed to exist and be admin
     const userId = req.params.id;
     db.users.delete({ id: userId });
     res.json({ ok: true });
   });
   ```

3. **For Express with inline checks**:
   ```javascript
   router.delete('/admin/users/:id', (req, res) => {
     if (!req.user) {
       return res.status(401).json({ error: 'Authentication required' });
     }
     if (!req.user.isAdmin) {
       return res.status(403).json({ error: 'Admin access required' });
     }
     // Now safe to proceed
     const userId = req.params.id;
     db.users.delete({ id: userId });
     res.json({ ok: true });
   });
   ```

4. **Apply the same pattern** to all four flagged routes:
   - `DELETE /admin/users/:id` in `routes/admin.js`
   - `POST /admin/posts/:id/feature` in `routes/admin.js`
   - `PUT /admin/settings` in `routes/admin.js`
   - `POST /comments/delete` in `queries.js`

5. **Admin routes need extra checks**: Since all three routes in `admin.js` are admin-only, you need to verify `req.user.isAdmin` (or whatever admin flag your app uses) in addition to checking authentication.

---

## Next steps

1. Read Claude's analysis above
2. Apply the changes yourself, in this branch or your main branch
3. Re-run a Trendslop audit to confirm the finding has passed
4. Close this PR when done

---

*Generated by [Trendslop](https://trendslop.ai). Need help? Contact support.*
