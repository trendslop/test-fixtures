# Trendslop AI-assisted fix instructions

> ⚠️ This was an AI-assisted fix attempt that fell back to instruction-only mode.
> Fallback reason: `Claude rated confidence LOW — won't ship code`

## Secrets in client-side code

**Severity:** HIGH
**Check ID:** `secrets_in_client`
**Summary:** 1 secret(s) embedded in client-side code.

---

## Claude's analysis and recommended fix

## Critical Security Issue: Anthropic API Key Exposed in Client-Side Code

### What's Leaking and Why It's Dangerous

**File:** `public/checkout.html`, line 9  
**Secret:** Anthropic API key (`sk-ant-FAKEKEY-trendslop-test-fixtures-not-real-abcdef0123456789`)

This key is hardcoded in JavaScript that ships directly to every user's browser. Anyone who loads your page can:
- Open DevTools → Sources tab → view the unminified script
- See the full API key in plain text
- Use it to make unlimited calls to Claude, racking up charges on your Anthropic account
- Continue using the key even after you "delete" it from your codebase, because they've already captured it

**Even if this is marked as a test key in the comment, treat it as compromised.** The pattern detection has flagged it, and if it's real, every user who loaded this page has a copy.

---

## Step 1: ROTATE THE SECRET IMMEDIATELY

1. Log in to your [Anthropic Console](https://console.anthropic.com/)
2. Navigate to API Keys
3. **Delete** the key `sk-ant-FAKEKEY-...` (or whatever the real key is)
4. Generate a new key
5. Store it somewhere safe temporarily (password manager, secure note) — you'll add it to your server environment in the next step

**Do not skip this step.** The old key has been public; rotating it is the only way to ensure attackers can't continue using it.

---

## Step 2: Move the Secret to Your Server

Since you're serving a static HTML file from `public/`, you need a backend to proxy the Anthropic API. Here's how to fix it based on common stacks:

### If you're using **Node.js / Express**:

1. **Install the Anthropic SDK:**
   html
<!DOCTYPE html>
<html>
<head><title>AI Chat</title></head>
<body>
  <button id="ask">Ask Claude</button>

  <script>
    document.getElementById('ask').addEventListener('click', async () => {
      try {
        // Call YOUR backend instead of Anthropic directly
        const resp = await fetch('/api/chat', { // Adjust path based on your setup
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ prompt: 'hi' })
        });
        
        if (!resp.ok) {
          throw new Error(`Server error: ${resp.status}`);
        }
        
        const data = await resp.json();
        console.log(data);
      } catch (error) {
        console.error('Failed to chat:', error);
      }
    });
  </script>
</body>
</html>
```

**Path adjustments:**
- Express standalone: `/api/chat`
- Next.js Pages Router: `/api/chat`
- Next.js App Router: `/api/chat`
- Netlify Functions: `/.netlify/functions/chat`
- Vercel Serverless: `/api/chat`

---

## Step 4: Verify and Test

1. **Local testing:**
   - Start your server with the new env var loaded
   - Open `http://localhost:3000` (or your dev server port)
   - Click "Ask Claude" — you should see a response in the console
   - Open DevTools → Network tab → confirm the request goes to `/api/chat`, not `api.anthropic.com`

2. **Check for other leaks:**
   - Search your entire `public/` folder for `sk-ant-`, API keys, or other secrets
   - Run a linter or secret scanner (e.g., `gitleaks`, `trufflehog`)

3. **Deploy:**
   - Push your changes (minus the `.env` file)
   - Set `ANTHROPIC_API_KEY` in your hosting provider's dashboard
   - Verify the deployed site works

---

## Why This Matters

Once rotated and fixed, your new Anthropic key is never visible in the browser. An attacker would need to compromise your server (much harder) instead of just opening DevTools (trivial). This pattern applies to **any** secret: Stripe keys, database credentials, OAuth client secrets — they all belong server-side.

---

## Next steps

1. Read Claude's analysis above
2. Apply the changes yourself, in this branch or your main branch
3. Re-run a Trendslop audit to confirm the finding has passed
4. Close this PR when done

---

*Generated by [Trendslop](https://trendslop.ai). Need help? Contact support.*
