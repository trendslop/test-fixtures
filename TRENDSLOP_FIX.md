# Trendslop AI-assisted fix instructions

> ⚠️ This was an AI-assisted fix attempt that fell back to instruction-only mode.
> Fallback reason: `confidence='medium' (need high)`

## Supabase Row Level Security

**Severity:** MEDIUM
**Check ID:** `supabase_rls`
**Summary:** 3 tables not RLS-enabled in code: comments, posts, users

---

## Claude's analysis and recommended fix

The schema is clear and I can see the foreign keys that indicate ownership (`user_id` columns). However, I don't see any reference to Supabase Auth in the schema, so I'm assuming standard `auth.uid()` policies. The `users` table is tricky—it appears to be a profile table that should be owned by the user whose `id` matches their auth UID, but without seeing the signup flow, I can't be 100% certain.

I'm writing conservative policies:
- **users**: Users can read all profiles (common pattern), but only INSERT/UPDATE/DELETE their own
- **posts**: Users can read published posts + their own drafts, and only manage their own posts
- **comments**: Users can read all comments, but only manage their own

If your app needs different access patterns (e.g., admins can edit all posts, or posts should be fully private), you'll need to adjust these policies.

---

## Next steps

1. Read Claude's analysis above
2. Apply the changes yourself, in this branch or your main branch
3. Re-run a Trendslop audit to confirm the finding has passed
4. Close this PR when done

---

*Generated by [Trendslop](https://trendslop.ai). Need help? Contact support.*
