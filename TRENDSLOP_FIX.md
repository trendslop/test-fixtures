# Trendslop AI-assisted fix instructions

> ⚠️ This was an AI-assisted fix attempt that fell back to instruction-only mode.
> Fallback reason: `Claude rated confidence LOW — won't ship code`

## Secrets in client-side code

**Severity:** HIGH
**Check ID:** `secrets_in_client`
**Summary:** 1 secret(s) embedded in client-side code.

---

## Claude's analysis and recommended fix

## Why this is LOW confidence

This fix requires creating a backend proxy endpoint, but you're serving a static `public/checkout.html` file. The context doesn't show what backend framework (if any) you're using — Node/Express, Next.js API routes, Python Flask, serverless functions, etc. I cannot write a usable proxy without knowing your stack.

Additionally, this is an **Anthropic API key** (not OpenAI as the summary mistakenly said). Anyone with this key can rack up charges on your Anthropic account by calling Claude models.

---

## Migration Instructions

### Step 1: Rotate the compromised key IMMEDIATELY

🚨 **The key `sk-ant-FAKEKEY-...` has been exposed to every user who loaded `checkout.html`.** Even though this is marked as a test fixture, if it were real, you'd need to:

1. Go to your [Anthropic Console](https://console.anthropic.com/) → API Keys
2. **Delete** the exposed key
3. **Create a new key** — do NOT reuse the old one

### Step 2: Understand what an attacker can do

With your Anthropic API key, an attacker can:
- Call Claude models (Opus, Sonnet, Haiku) and rack up charges on your account
- Exhaust your rate limits
- Use your key for their own projects at your expense

Anthropic keys are **not scoped** to specific domains or IP ranges, so once leaked, they're fully compromised.

### Step 3: Create a backend proxy endpoint

You need to move the API call server-side. Here's what that looks like for **common stacks**:

#### If you're using **Node.js/Express**:

Create a new file `server.js` (or add to your existing Express app):



Set the environment variable:

ANTHROPIC_API_KEY=sk-ant-YOUR-NEW-KEY
```

#### If you're using **Next.js**:

Create `pages/api/chat.js` (or `app/api/chat/route.js` for App Router):



**Critical Next.js gotcha**: Environment variables prefixed with `NEXT_PUBLIC_` are embedded in the client bundle. For secrets, use **unprefixed** variables — they're only available server-side.

In `.env.local`:
```
ANTHROPIC_API_KEY=sk-ant-YOUR-NEW-KEY
```

#### If you're using **serverless functions** (Vercel, Netlify, AWS Lambda):

Create `api/chat.js` (Vercel) or `netlify/functions/chat.js` (Netlify):



Then configure the secret in your hosting provider:
- **Vercel**: Dashboard → Project Settings → Environment Variables → Add `ANTHROPIC_API_KEY`
- **Netlify**: Site Settings → Build & Deploy → Environment → Add `ANTHROPIC_API_KEY`

### Step 4: Update your client code

Replace `public/checkout.html` with:



Notice:
- No `ANTHROPIC_KEY` variable
- Fetch goes to `/api/chat` (your backend) instead of `https://api.anthropic.com`
- The backend handles the Anthropic API call with the secret

### Step 5: Deploy with the new secret

1. Add `ANTHROPIC_API_KEY=sk-ant-YOUR-NEW-KEY` to your environment variables (see Step 3 for your platform)
2. Deploy your backend + updated HTML
3. Test in DevTools — confirm the Anthropic key is **not** visible in the Network tab or Sources

### Step 6: Add rate limiting (optional but recommended)

Since your backend is now public-facing, consider:
- Rate limiting per IP (e.g., `express-rate-limit`)
- Authentication (require users to log in before calling `/api/chat`)
- Usage caps (track how many requests each user makes)

Otherwise, an attacker can still abuse your endpoint by spamming `/api/chat` — you've just moved the attack vector from "steal the key" to "spam the proxy."

---

## What you need to tell me to write the code

If you want a HIGH confidence fix with complete code, reply with:

1. **What backend framework are you using?** (Express, Next.js, Vercel functions, Python Flask, etc.)
2. **Where is your backend code located?** (file paths)
3. **Do you want authentication on the `/api/chat` endpoint?** (yes/no)

Then I can write production-ready code for your exact setup.

---

## Next steps

1. Read Claude's analysis above
2. Apply the changes yourself, in this branch or your main branch
3. Re-run a Trendslop audit to confirm the finding has passed
4. Close this PR when done

---

*Generated by [Trendslop](https://trendslop.ai). Need help? Contact support.*
